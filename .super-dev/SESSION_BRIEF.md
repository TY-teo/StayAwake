# Session Brief - ScreenSleepControl

ACTIVE_WORKFLOW: Super Dev (standard mode)
CURRENT_GATE: docs_confirm — 等待用户确认三份核心文档后才创建 Spec / 开始编码。

用户在此回复"确认/通过/继续"= 通过文档门进入 spec；回复"修改/补充/具体反馈"= 留在 docs 阶段更新文档后再次等待确认。

项目: Mac 菜单栏工具，两功能(保持唤醒 / 合盖继续运行)，macOS 26 Liquid Glass。显示名「StayAwake」。
已确认决策: 合盖=方案A 管理员弹窗; 屏幕策略默认"允许屏幕关闭"; MVP 含定时自动关闭。
仍待: 用户对文档整体的最终放行(确认后进入 spec -> 前端优先)。
